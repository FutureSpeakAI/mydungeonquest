---
name: Dowry grounding law
description: Outside-lore proposals — citations must ground NAMES whole-phrase; the amend bypass is two-handed
---
- A citation court that only checks quote-appears-on-page is a doorframe with no door: bind the proposal's NAME to its quote. And substring `includes` is NOT binding — `ella` hides inside "Sella Marrow"; demand whole-phrase presence, bounded by non-letters (`\p{L}\p{N}`, u-flag) on both sides or line edges.
- **Why:** two architect sittings convicted in sequence: (γ) an invented soul riding an unrelated but perfectly verbatim line passed every court; (δ) an embedded-substring name riding a true line passed the γ cure. Both greened under the prior law before the cure (red-first observed each time).
- Player amendments lawfully exceed the page (rename sovereignty, the hero-forge precedent). The bypass is TWO-HANDED: a context flag only first-party caller code turns (never row data — signature-downgrade law) AND a per-row provenance mark minted ONLY by the UI's rename hand, dropped at the wire-boundary re-seat, sealed into the journal payload as provenance. Unamended rows keep grounding at EVERY seat — the threshold included — so upstream strictness is never a single point of failure.
- **How to apply:** any court where reader/AI output proposes named entities from quoted sources; any "the player may override" bypass — never let one hand alone (context flag or row flag) open it.

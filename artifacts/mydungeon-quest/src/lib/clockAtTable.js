// Compat seat — THE CLOCK AT THE TABLE moved home with the parity
// cut; the engine answers from its true seat (fatescript/clock), the
// Calendar Law and its table words together.
export * from 'fatescript/clock';
